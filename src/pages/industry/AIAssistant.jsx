import { BotMessageSquare, Plus, Send, Trash2 } from 'lucide-react'
import { useState } from 'react'
import IndustryLayout from '../../layouts/IndustryLayout'

const suggestions = ['Which standard applies to my product?', 'Do I need BIS certification?', 'What documents are required?']
const initialMessages = [{ role: 'ai', text: 'Hello, Ananya. I can help you explore BIS standards, certification requirements and compliance steps. What would you like to understand?' }, { role: 'user', text: 'What is required to certify an electric ceiling fan?' }, { role: 'ai', text: 'For an electric ceiling fan, you can begin by reviewing the applicable product standard, preparing technical documentation and arranging required testing. I have listed relevant sources alongside this conversation.' }]
function SourceCard({ title, subtitle }) { return <article className="rounded-xl border border-slate-200 p-4"><p className="text-sm font-bold text-navy">{title}</p><p className="mt-1 text-xs leading-5 text-slate-500">{subtitle}</p><button type="button" className="mt-3 text-xs font-bold text-navy hover:underline">View source</button></article> }
export default function AIAssistant() { const [messages, setMessages] = useState(initialMessages); const [input, setInput] = useState(''); const send = () => { if (!input.trim()) return; setMessages([...messages, { role: 'user', text: input }, { role: 'ai', text: 'This is a demo response. In a later phase, this workspace will provide source-aware guidance based on BIS information.' }]); setInput('') }; return <IndustryLayout title="AI Assistant"><div className="grid gap-6 xl:grid-cols-[minmax(0,1fr)_310px]"><section className="flex min-h-[680px] flex-col overflow-hidden rounded-2xl border border-slate-200 bg-white"><div className="flex items-center justify-between border-b border-slate-100 px-5 py-4"><div className="flex items-center gap-3"><span className="grid h-9 w-9 place-items-center rounded-lg bg-blue-50 text-navy"><BotMessageSquare size={19} /></span><div><h2 className="font-bold text-ink">BIS AI Assistant</h2><p className="text-xs text-slate-500">Demo guidance · sources shown separately</p></div></div><button type="button" onClick={() => setMessages([])} className="inline-flex items-center gap-2 rounded-lg border border-slate-200 px-3 py-2 text-xs font-bold text-slate-600 hover:text-navy"><Trash2 size={14} />Clear</button></div><div className="flex-1 space-y-5 overflow-y-auto bg-slate-50/70 p-5">{messages.map((message, index) => <div key={index} className={`flex ${message.role === 'user' ? 'justify-end' : 'justify-start'}`}><div className={`max-w-[85%] rounded-2xl px-4 py-3 text-sm leading-6 ${message.role === 'user' ? 'rounded-br-md bg-navy text-white' : 'rounded-bl-md border border-slate-200 bg-white text-slate-700'}`}>{message.text}</div></div>)}{messages.length === 0 && <p className="py-20 text-center text-sm text-slate-500">Start a new conversation using one of the suggestions below.</p>}</div><div className="border-t border-slate-100 p-4"><div className="mb-3 flex flex-wrap gap-2">{suggestions.map((item) => <button onClick={() => setInput(item)} key={item} type="button" className="rounded-full border border-slate-200 px-3 py-1.5 text-xs text-slate-600 hover:border-blue-200 hover:text-navy">{item}</button>)}</div><div className="flex gap-2 rounded-xl border border-slate-200 p-2 focus-within:border-navy"><input value={input} onChange={(e) => setInput(e.target.value)} onKeyDown={(e) => e.key === 'Enter' && send()} className="min-w-0 flex-1 border-0 px-2 text-sm outline-none" placeholder="Ask about standards, certification or compliance..." /><button onClick={send} type="button" className="grid h-9 w-9 place-items-center rounded-lg bg-navy text-white" aria-label="Send message"><Send size={17} /></button></div></div></section><aside className="rounded-2xl border border-slate-200 bg-white p-5"><h2 className="font-bold text-ink">Sources</h2><p className="mt-1 text-xs leading-5 text-slate-500">Reference cards for this demo response.</p><div className="mt-5 space-y-3"><SourceCard title="IS 374: 2019" subtitle="BIS Standard · Electric ceiling fans" /><SourceCard title="BIS Certification Guidelines" subtitle="Official BIS document · Product certification" /></div><button type="button" onClick={() => setMessages([])} className="mt-5 inline-flex items-center gap-2 text-sm font-bold text-navy"><Plus size={16} />New conversation</button></aside></div></IndustryLayout> }
