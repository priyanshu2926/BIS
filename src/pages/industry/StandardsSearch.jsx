import { Filter, Search } from 'lucide-react'
import { useMemo, useState } from 'react'
import IndustryLayout from '../../layouts/IndustryLayout'
import { StandardCard } from '../../components/industry/IndustryUI'
import { standards } from '../../data/industryMockData'

export default function StandardsSearch() { const [query, setQuery] = useState(''); const [category, setCategory] = useState('All'); const results = useMemo(() => standards.filter((s) => `${s.number} ${s.title} ${s.category}`.toLowerCase().includes(query.toLowerCase()) && (category === 'All' || s.category === category)), [query, category]); return <IndustryLayout title="Standards Search"><section className="rounded-2xl border border-slate-200 bg-white p-5 sm:p-7"><p className="text-sm font-semibold text-saffron">BIS standards explorer</p><h2 className="mt-2 text-2xl font-bold text-ink">Find the standard relevant to your product</h2><div className="mt-6 flex flex-col gap-3 sm:flex-row"><div className="flex flex-1 items-center gap-3 rounded-xl border border-slate-200 px-4 focus-within:border-navy"><Search size={20} className="text-slate-400" /><input value={query} onChange={(e) => setQuery(e.target.value)} className="w-full border-0 py-3 text-sm outline-none" placeholder="Search by standard number, product or keyword" /></div><button type="button" className="rounded-xl bg-navy px-6 py-3 text-sm font-bold text-white">Search</button></div></section><div className="mt-6 grid gap-6 lg:grid-cols-[240px_minmax(0,1fr)]"><aside className="h-fit rounded-2xl border border-slate-200 bg-white p-5"><div className="flex items-center gap-2 font-bold text-ink"><Filter size={18} />Filters</div><label className="mt-5 block text-xs font-bold uppercase tracking-wide text-slate-500">Category<select value={category} onChange={(e) => setCategory(e.target.value)} className="mt-2 w-full rounded-lg border border-slate-200 bg-white p-2.5 text-sm text-slate-700"><option>All</option>{[...new Set(standards.map((s) => s.category))].map((item) => <option key={item}>{item}</option>)}</select></label><label className="mt-4 block text-xs font-bold uppercase tracking-wide text-slate-500">Product<select className="mt-2 w-full rounded-lg border border-slate-200 bg-white p-2.5 text-sm text-slate-700"><option>All products</option><option>Electric Ceiling Fan</option><option>Electrical appliances</option></select></label><label className="mt-4 block text-xs font-bold uppercase tracking-wide text-slate-500">Status<select className="mt-2 w-full rounded-lg border border-slate-200 bg-white p-2.5 text-sm text-slate-700"><option>All statuses</option><option>Active</option><option>Under review</option></select></label></aside><section><div className="mb-4 flex items-center justify-between"><p className="text-sm text-slate-600"><b className="text-ink">{results.length}</b> mock standards found</p><span className="text-xs text-slate-400">Demo data only</span></div><div className="grid gap-4 xl:grid-cols-2">{results.map((standard) => <StandardCard key={standard.number} standard={standard} />)}{!results.length && <p className="rounded-xl border border-dashed border-slate-300 p-10 text-center text-sm text-slate-500 xl:col-span-2">No standards match this search.</p>}</div></section></div></IndustryLayout> }
